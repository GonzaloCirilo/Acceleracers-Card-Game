namespace AcceleracersCCG.Effects
{
    /// <summary>
    /// When a card effect triggers.
    /// </summary>
    public enum EffectTrigger
    {
        OnEquip,
        OnRemove,
        OnAdvance,
        OnTuneUp,
        OnJunked,
        Passive
    }
}
